@model StoreLogic.Entities.Cake

<div class="well">
    <h3>
        <strong>
            @Model.Name
        </strong>
        @if (@Model.Category.Equals("Кекс"))
        {
            <span class="badge badge-danger">New!</span>
        }

        <span class="pull-right label label-primary">@Model.Price$ USD</span>
    </h3>
    <span class="lead">@Model.Description</span>
</div>